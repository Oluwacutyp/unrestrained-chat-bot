# God Quant — ALL dependencies are OPTIONAL. Stdlib-only core works as-is.
# Install what your device can handle:
#   pip install -r requirements.txt        # everything
#   pip install requests rich pytest       # recommended for Termux
requests>=2.28
numpy>=1.24
rich>=13.0
pytest>=7.0
yfinance>=0.2.0   # optional: extra data source
